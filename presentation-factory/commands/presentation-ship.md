---
description: Run only Stage 4 (Ship) — render HTML slides to PDF, convert PDF to editable PPTX with movable assets, promote to canonical destination, archive WIP.
---

Invoke the `presentation-factory-ship` skill. Render slides to PDF (Chromium headless), then convert to editable PowerPoint (text editable, assets movable). Promote to destination based on intent. Archive WIP.

Output: `deck.pptx` + `deck.pdf` at the chosen canonical destination. WIP archived.
