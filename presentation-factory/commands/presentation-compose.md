---
description: Run only Stage 3 (Compose) — generate assets (Higgsfield / Napkin / Flaticon), compose HTML slides from templates with brand tokens inlined. Output: slides/*.html + deck.html index.
---

Invoke the `presentation-factory-compose` skill. Generate every image / infographic / icon referenced in `10_Content.md`. Then run the slide composer to produce one HTML file per slide plus the `deck.html` review index.

Output: `WIP/[deck-name]/slides/NN-template.html` (×N) and `WIP/[deck-name]/slides/deck.html`.
