---
description: Start a new presentation end-to-end (Frame → Draft → Compose → Ship). Default entry point for the Presentation Factory.
---

Invoke the `presentation-factory-create-flow` skill. Walk the user through the full pipeline.

If the user has already started a deck (a WIP folder exists), surface it and ask whether to continue or start fresh.
